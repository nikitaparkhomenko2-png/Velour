const defaults={name:"Mercedes-Benz G-Класс AMG",plate:"A 001 AA 01",fines:"0",price:"31 738 529 ₽",priceLabel:"Цена Mercedes-Benz G-Класс AMG",fuel:"55 л",fuelPercent:"78%"};const saved=JSON.parse(localStorage.getItem("autoglass-data")||"null")||defaults;document.querySelectorAll("[data-key]").forEach(el=>{let k=el.dataset.key;el.textContent=saved[k]??defaults[k];el.addEventListener("blur",()=>{saved[k]=el.textContent.trim();localStorage.setItem("autoglass-data",JSON.stringify(saved));if(k==="fuelPercent")updateFuel(el.textContent)});el.addEventListener("keydown",e=>{if(e.key==="Enter"){e.preventDefault();el.blur()}})});function updateFuel(v){let n=parseInt(String(v).replace(/\D/g,""),10)||0;n=Math.max(0,Math.min(100,n));document.getElementById("fuelFill").style.width=n+"%"}updateFuel(saved.fuelPercent);const fines=document.querySelector('[data-key="fines"]');function updateFines(){let n=parseInt(fines.textContent.replace(/\D/g,""),10)||0;fines.style.color=n>0?"#ff4d57":"#fff"}updateFines();fines.addEventListener("input",updateFines);
const DB="AutoGlassDB",STORE="photos",KEY="car";function db(){return new Promise((res,rej)=>{let r=indexedDB.open(DB,1);r.onupgradeneeded=()=>r.result.createObjectStore(STORE);r.onsuccess=()=>res(r.result);r.onerror=()=>rej(r.error)})}async function getPhoto(){try{let d=await db();return await new Promise((res,rej)=>{let r=d.transaction(STORE).objectStore(STORE).get(KEY);r.onsuccess=()=>res(r.result);r.onerror=()=>rej(r.error)})}catch(e){return null}}async function putPhoto(f){let d=await db();return new Promise((res,rej)=>{let t=d.transaction(STORE,"readwrite");t.objectStore(STORE).put(f,KEY);t.oncomplete=res;t.onerror=()=>rej(t.error)})}const img=document.getElementById("carImage"),input=document.getElementById("photoInput");img.src="https://upload.wikimedia.org/wikipedia/commons/f/fe/Mercedes-Benz_G-Class.jpg";getPhoto().then(f=>{if(f)img.src=URL.createObjectURL(f)});input.addEventListener("change",async e=>{let f=e.target.files?.[0];if(!f)return;try{await putPhoto(f);img.src=URL.createObjectURL(f);toast("Фото установлено")}catch(x){toast("Ошибка сохранения")}input.value=""});function toast(m){let t=document.getElementById("toast");t.textContent=m;t.classList.add("show");setTimeout(()=>t.classList.remove("show"),1300)}